FROM python:3.9-slim

WORKDIR /app
COPY app.py .

# Install Flask + Gunicorn for production
RUN pip install flask gunicorn

EXPOSE 80

# Use Gunicorn WSGI server
CMD ["gunicorn", "-w", "1", "-b", "0.0.0.0:80", "app:app"]
